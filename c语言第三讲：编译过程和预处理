预处理：头文件的替换，宏的替换，取消注释。
 gcc -E 1.c -o 1.i
编译：将1.i文件通过编译器变成汇编代码，（语法检查）
 gcc -S 1.i -o 1.s
汇编：将汇编代码生成目标文件（可重定向文件）
 gcc -c 1.s -o 1.o

hexdump -C 1.o
ELF 可执行可连接的文件

链接：最终生成可执行文件：
gcc 1.o -o 1


// 单行注释

 
多行注释

#if 0
#endif
条件注释

在c语言中 0代表假 非0代表真
